# Environment variables for Chatbot Backend (Django)
#
# Copy this file to .env and edit values as needed.
# Note: Do NOT commit real secrets to version control.

# OpenAI API key (required for /api/chat endpoint to function)
OPENAI_API_KEY=your-openai-api-key-here

# Hosts allowed to access Django (comma-separated)
# Examples:
#   ALLOWED_HOSTS=localhost,127.0.0.1,chatbot_frontend
#   ALLOWED_HOSTS=*.example.com,api.example.com
# By default (when unset), the app allows all hosts in development.
# For production, set this explicitly and remove "*".
# ALLOWED_HOSTS=
