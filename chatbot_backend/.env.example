# Backend environment variables

# OpenAI API Key (required only for the /api/chat/ endpoint to work).
# The server will still start and /api/health/ will work without this, but chat will return an error.
OPENAI_API_KEY=

# Optional: Override Django settings if needed
# DEBUG=true
# ALLOWED_HOSTS=localhost,127.0.0.1
